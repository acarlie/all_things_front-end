
<header class="row row--header">
    <div class="container">
        <h1>All Things <span class="text--gradient">&#123;</span> CSS <span class="text--gradient">&#125;</span></h1>
        <button class="btn btn--primary btn--darkBg btn--pill"><span class="btn__span"><a href="/scrape">Load New Articles!</a></span></button>
        <button class="btn btn--accent btn--darkBg btn--pill"><span class="btn__span"><a href="#">❤ View Saved</a></span></button>
    </div>
</header>

<main class="row row--body">
    <div class="container">
        {{#if success}}
            {{#each articles}}
                {{> article}}
            {{/each}}
        {{else}}
            <h5>No articles found</h5>
            <button class="btn btn--primary"><span class="btn__span"><a href="/scrape">Load New Articles!</a></span></button>
        {{/if}}
    </div>
</main>

